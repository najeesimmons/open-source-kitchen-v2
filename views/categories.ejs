<!DOCTYPE html>
<html lang="en">

    <%- include ('./partials/head.ejs', {page: 'Recipe Blog' }) %>

    <%- include ('./partials/nav.ejs') %>

            <h1>
                <%= category %>
            </h1>
            <main>
                <div>
                    <% for(let idx=0; idx < foundCategory.length; idx++){ %>
                        <div>
                            <article>
                                <section>
                                    <figure>
                                    </figure>
                        </div>
                        </section>
                        <div>
                            <div>
                                <div>
                                    <img src="<%= foundCategory[idx].image %>" />
                                </div>
                                <a href="/recipeblog/<%= foundCategory[idx].title %>">
                                    <div>
                                        <h2>
                                            <%= foundCategory[idx].title %>
                                        </h2>
                                    </div>
                                </a>
                                <div>
                                    <%= foundCategory[idx].description %>
                                </div>
                            </div>
                        </div>
                        </article>
                </div>
                <% } %>
                    </div>
            </main>

        </body>

</html>